#include<stdio.h>
#include<vector>
#include<algorithm>

int main()
{
    int x,y,i;
    vector<int>a;
    while(scanf("%d",&x)&&x){
        for(i=0;i<x;i++){
            scanf("%d",&y);
            a.push_back(y);
        }
        sort(a.begin(),a.end());
        for(i=0;i<6;i++){

        }
    }

}
